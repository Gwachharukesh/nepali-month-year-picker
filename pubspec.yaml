name: nepali_month_year_picker
description: The month year picker inspired by the Flutter's date picker design.
version: 0.0.5+5
homepage: https://github.com/kojurohan42/nepali-month-year-picker.git

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  nepali_utils: ^3.0.8

dev_dependencies:
  flutter_lints: ^4.0.0

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:


